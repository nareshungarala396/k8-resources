apiVersion: v1
kind: Pod
metadata:
  name: pod-LB-service-demo
    # These labels are used to register this pod with service
  labels:
    project: roboshop
    component: frontend
    environment: dev
    purpose: loadbalancer
spec:
  containers:
  - name: nginx
    image: nginx